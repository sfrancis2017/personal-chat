{
  "name": "chat-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret:claude": "wrangler secret put ANTHROPIC_API_KEY",
    "secret:openai": "wrangler secret put OPENAI_API_KEY",
    "secret:db": "wrangler secret put DATABASE_URL"
  },
  "dependencies": {
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.78.0"
  }
}
